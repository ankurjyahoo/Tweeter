<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.yahoo.learn.android.tweeter.activities.ComposeActivity">
    <item android:id="@+id/action_settings" android:title="@string/action_settings"
        android:orderInCategory="100" app:showAsAction="never" />


    <item android:id="@+id/action_submit_tweet"
        android:title="Submit"
        android:icon="@drawable/ic_tweet"
        android:orderInCategory="3"
        app:showAsAction="always"
        android:onClick="onMenuSubmit"
        />

    <item android:id="@+id/action_cancel_tweet"
        android:title="Cancel"
        android:icon="@drawable/ic_cancel"
        android:orderInCategory="2"
        app:showAsAction="always"
        android:onClick="onMenuCancel"
        />


</menu>
