<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.yahoo.learn.android.tweeter.activities.HomeActivity">
    <item android:id="@+id/action_settings" android:title="@string/action_settings"
        android:orderInCategory="100" app:showAsAction="never" />

    <item android:id="@+id/action_post_tweet"
        android:title="Post"
        android:icon="@drawable/ic_twitter_quill"
        android:orderInCategory="3"
        app:showAsAction="always"
        android:onClick="postTweet"
    />

    <item android:id="@+id/action_profile"
        android:title="Post"
        android:icon="@drawable/ic_profile"
        android:orderInCategory="2"
        app:showAsAction="always"
        android:onClick="onProfileView"
    />

</menu>
